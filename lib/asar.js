// Generated by CoffeeScript 1.8.0
var AsarArchive, createArchive, extractArchive, getEntries, loadArchive, opts;

AsarArchive = require('./archive');

opts = {};

createArchive = function(srcDir, archiveFilename, cb) {
  var archive;
  archive = new AsarArchive(opts);
  if (srcDir != null) {
    archive.addDirectory(srcDir, srcDir, function(err) {
      if (archiveFilename != null) {
        return archive.write(archiveFilename, {}, function(err) {
          return cb(err, archive);
        });
      }
    });
  }
};

loadArchive = function(archiveFilename) {
  var archive;
  archive = new AsarArchive(opts);
  archive.openSync(archiveFilename);
  return archive;
};

getEntries = function(archiveFilename, archiveRoot) {
  var archive, list;
  if (archiveRoot == null) {
    archiveRoot = '/';
  }
  archive = loadArchive(archiveFilename);
  list = archive.getEntries(archiveRoot);
  return list;
};

extractArchive = function(archiveFilename, destDir, archiveRoot) {
  var archive;
  if (archiveRoot == null) {
    archiveRoot = '/';
  }
  archive = loadArchive(archiveFilename);
  archive.extractSync(destDir, archiveRoot);
};

module.exports = {
  AsarArchive: AsarArchive,
  createArchive: createArchive,
  loadArchive: loadArchive,
  getEntries: getEntries,
  extractArchive: extractArchive,
  opts: opts
};
